/**
 * Tipos de datos estáticos para el contenido de la Landing Page.
 */

export interface ClassCardData {
  id: string;
  title: string;
  description: string;
  image: string;
  imageAlt: string;
  badge?: string;
  whatsappMessage?: string;
  mapsUrl?: string;
}
